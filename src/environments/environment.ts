// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyCfHDQqWjf7KaLPhutuqzs56rporJsI9tY',
    authDomain: 'rezeptbuch-80e65.firebaseapp.com',
    databaseURL: 'https://rezeptbuch-80e65.firebaseio.com',
    projectId: 'rezeptbuch-80e65',
    storageBucket: 'gs://rezeptbuch-80e65.appspot.com',
    messagingSenderId: '866271807595',
    appId: '1:866271807595:web:1e4168cfc7737d8c'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
